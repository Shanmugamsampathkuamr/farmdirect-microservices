server:
  port: 8087

spring:
  application:
    name: location-service
  datasource:
    url: jdbc:postgresql://localhost:5432/farmdirect_locations
    username: postgres
    password: sampath12222@

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect


eureka:
  instance:
    prefer-ip-address: true
  client:
    service-url:
      defaultZone: http://discovery-service:8761/eureka/